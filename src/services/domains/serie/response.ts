export interface SerieListaResponse {
  id: number;
  nome: string;
}

export interface SerieDetalheResponse {
  id: number;
  nome: string;
}

export interface SerieResponse {
  id: number;
  nome: string;
}
